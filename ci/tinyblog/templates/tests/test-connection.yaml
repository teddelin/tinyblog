apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tinyblog.fullname" . }}-test-connection"
  labels:
    {{- include "tinyblog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tinyblog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
